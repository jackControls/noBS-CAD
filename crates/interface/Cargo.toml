[package]
name = "nbcad-interface"
version.workspace = true
edition = "2021"
description = "Shared product catalog and live control semantics"
license = "LGPL-2.1-or-later"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
