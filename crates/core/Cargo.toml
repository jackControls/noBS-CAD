[package]
name = "nbcad-core"
version = "0.1.0"
edition = "2021"
description = "noBS CAD document model, stable topology ids, browser tree, and feature history"
license = "LGPL-2.0-or-later"
repository = "https://github.com/jackControls/noBS-CAD"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
