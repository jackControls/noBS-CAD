[package]
name = "nbcad-mcp-mutate"
version = "0.1.0"
edition = "2021"
description = "Shared MCP modeling-mutate name → engine method + payload map"
license = "LGPL-2.1-or-later"
repository = "https://github.com/jackControls/noBS-CAD"

[dependencies]
serde_json = "1"
