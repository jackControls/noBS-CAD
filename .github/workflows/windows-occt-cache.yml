name: Warm Windows ARM SDK cache

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'vcpkg.json'
      - '.github/actions/setup-windows-occt/**'
      - '.github/workflows/windows-occt-cache.yml'
  # Refresh the default-branch cache after runner/compiler updates and keep it
  # available between releases. A warm run only restores the SDK, not the app.
  schedule:
    - cron: '17 6 * * *'

permissions:
  contents: read

concurrency:
  group: windows-arm-sdk-cache
  cancel-in-progress: false

jobs:
  warm-arm64:
    # Never seed a PR's build products into the default branch cache namespace.
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    runs-on: windows-11-vs2026-arm
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - name: Warm shared ARM64 OpenCASCADE SDK
        uses: ./.github/actions/setup-windows-occt
        with:
          triplet: arm64-windows
          runner-cache-key: windows-11-vs2026-arm-arm64-windows-msvc
          msvc-component: Microsoft.VisualStudio.Component.VC.Tools.ARM64
